[package]
name = "rust_gomoku_android"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"

[lib]
crate-type = ["cdylib"]

[dependencies]
rust_gomoku = { path = "../.." }

[profile.release]
lto = "fat"
codegen-units = 1
panic = "abort"
